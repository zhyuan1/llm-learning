---
type: framework
style: minimal-flat
palette: default-soft
language: zh
aspect: 16:9
output: ../01-framework-context-memory-gap.png
---

Create a clean minimal-flat editorial infographic for a Chinese technical WeChat article.

TITLE: “有限窗口 vs 长期任务”

VISUAL STRUCTURE:

- Left side: a compact rectangular box labeled “Context Window（本轮可见）”. Inside it show small stacked cards: “system
  prompt”, “当前对话”, “工具结果”, “压缩摘要”.
- Right side: a long horizontal timeline labeled “长期任务 / 多轮协作”, with small milestones: “昨天偏好”, “上周项目”,
  “历史决策”, “未来任务”.
- Center: a layered memory system hub labeled “Memory System”. It routes information between the long timeline and the
  current context window.
- Show three arrows from Memory System into Context Window: “常驻”, “按需召回”, “压缩后保留”.
- Show one arrow from Context Window back into Memory System: “写入 / 晋升”.

KEY MESSAGE TEXT:

- Main caption at bottom: “记住，不等于已经进入本轮上下文”
- Small annotation: “问题不只是存储，而是调度”

STYLE:

- Minimal flat vector, soft off-white background, rounded rectangles, thin lines, subtle shadows.
- Use blue-gray as base, warm orange accent for “Memory System”, green accent for “按需召回”.
- No 3D, no photorealism, no clutter.

TYPOGRAPHY:

- Chinese labels must be clear and legible.
- Use simple sans-serif style.

COMPOSITION:

- 16:9, generous whitespace, suitable for inline WeChat article reading.
