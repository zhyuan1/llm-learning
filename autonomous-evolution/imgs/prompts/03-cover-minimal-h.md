---
type: minimal
palette: dark
rendering: flat-vector
text: title-subtitle
mood: subtle
font: clean
aspect: 2.35:1
lang: zh
---

A minimal flat-vector cover image. Dark background #0d1117. Wide cinematic 2.35:1 ratio. HORIZONTAL text only, no
vertical text.

## Visual

Left-center area: one thin glowing teal circle (#00e5cc), clean stroke, no fill. A single small arrow on the ring
indicating rotation direction. Faint echo ring slightly larger at 10% opacity. Nothing else.

## Text layout — HORIZONTAL, left to right

Right half of canvas, vertically centered, all text HORIZONTAL (left-to-right reading direction):

- Line 1: "自主进化" — large bold white sans-serif, horizontal
- Line 2 below: "当 AI 开始为自己写代码" — smaller gray #888888, horizontal

Both lines left-aligned, stacked vertically, horizontal reading direction.

## Hard rules

- All text MUST be horizontal, left-to-right. NO vertical text. NO rotated text.
- One ring only. No rainbow. No labels on the ring.
- No texture, no grid, no decorations beyond the echo ring.
